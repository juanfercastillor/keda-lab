apiVersion: v1
kind: Service
metadata:
  name: keda-lab
  namespace: keda-lab
  labels:
    app.kubernetes.io/name: keda-lab
spec:
  selector:
    app.kubernetes.io/name: keda-lab
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
